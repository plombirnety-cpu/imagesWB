# Зависимости панели поверх зависимостей движка (см. ../requirements.txt —
# устанавливается ПЕРВЫМ, тут только то, чего движку не хватает).
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
loguru>=0.7
