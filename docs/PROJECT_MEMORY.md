# Память проекта Print Factory

## Премиальные товарные сцены с чёрной футболкой (2026-08-24)

- Через Gemini I2I создано 4 разных варианта товарной сцены с чёрной футболкой
  размером 928×1152. Получено ровно 4 успешных image outputs, без лишних
  платных повторов.
- Готовые файлы лежат локально в
  `C:\Users\Man99\OneDrive\Рабочий стол\готовые`; резервная серверная копия —
  в `/opt/print-panel/manual-results/tshirt-flatlay-20260824`.
- Контроль качества: заголовок `PROTECTED WITNESS` сохранён, watermark и UI
  отсутствуют, четыре результата различаются фоном и размещением футболки.
- Для белой футболки создана вторая ручная партия из 3 премиальных Gemini I2I
  вариантов: тёплый травертин, тёмный графит и глубокий бордовый фон. Менялись
  только фон и небольшое положение изделия.
- Файлы `05_white_hoodbyair_warm_travertine.png`,
  `06_white_hoodbyair_dark_graphite.png` и
  `07_white_hoodbyair_deep_burgundy.png` лежат в
  `C:\Users\Man99\OneDrive\Рабочий стол\готовые`; резервная серверная копия —
  `/opt/print-panel/manual-results/white-tee-backgrounds-20260824`.
- Проверено визуально: `CLASSICS`, `HOOD BY AIR`, череп, звёзды и оба знака на
  рукавах сохранены, watermark Avito и точка карусели удалены. Было ровно 3
  успешных платных image outputs; первый технический запуск остановился до API.
- Это отдельная ручная партия результатов: production-код и deploy не менялись.

## Handoff Telegram → товар → Диск → фотостудия → выкладка (2026-08-22)

- Каноническое задание следующему ИИ/разработчику хранится в
  `docs/TELEGRAM_ASSISTANT_HANDOFF.md`; там есть готовый текст владельца,
  endpoints, FSM-кнопки, имена файлов, таблицы БД, manifest, этапы и acceptance
  criteria.
- Print Factory не управляет артикулами, Яндекс Диском, ролями карточки и
  публикацией. Это новая зона ответственности Telegram-бота/товарного модуля;
  не добавлять фиктивные вызовы в Print Factory.
- `products.article` в БД — источник истины. Имя файла и manifest только
  проверяются против него. После публикации article неизменяем; seller SKU
  размеров/цветов хранится отдельно.
- На товар и профиль площадки допускается ровно одна активная `main`; остальные
  одобренные кадры имеют стабильный `gallery_order`. `rejected` не экспортируется,
  `archive` сохраняется, `PRINT_ONLY` означает завершённое хранение без модели,
  но не готовность к публикации.
- Платный POST нельзя автоматически повторять после timeout: API ещё не имеет
  серверной идемпотентности. Локальный submit-lock предотвращает двойной callback,
  а сетевую неопределённость переводит в `submission_unknown` для ручной проверки.
- Перед боевой передачей Bearer между разными серверами нужен HTTPS, VPN или
  SSH-туннель; IP-allowlist `72.56.67.18` сам по себе трафик не шифрует.

## Машинный API Telegram-бота: Bearer + IP allowlist (2026-08-22)

- Канонический IP сервера Telegram-бота группы «Маркетплейсы» — `72.56.67.18`.
  Production allowlist должен также сохранять `127.0.0.1` и `::1` для локального
  smoke-теста и аварийной диагностики.
- Машинная авторизация двухуровневая: при заданном
  `PRINT_FACTORY_SERVICE_ALLOWED_IPS` запрос обязан одновременно прийти с адреса
  из allowlist и содержать правильный `Authorization: Bearer
  <PRINT_FACTORY_SERVICE_TOKEN>`. Не принимать `X-Forwarded-For` от публичного
  клиента как доказательство IP.
- Сервисному клиенту разрешены все `/api/*`, а также защищённые `/openapi.json`,
  `/docs`, `/redoc` и `/docs/oauth2-redirect`. Токен не является паролем владельца
  и никогда не должен открывать HTML-панель `/`.
- Точка первичной диагностики интеграции — `GET /api/capabilities`; последние
  generation/studio job-ы доступны через `GET /api/jobs`. Job-регистр остаётся
  in-memory: после рестарта бот начинает новый job, а не полагается на старый
  статус.
- Preview постоянных фотомоделей нельзя отдавать через незащищённый `/static/`:
  `image_url` каталога ведёт на `/api/studio/models/{model_id}/image` и требует ту
  же авторизацию.
- Не передавать реальный сервисный токен в Git, документацию, логи или чат.
  Allowlist уменьшает поверхность атаки, но не скрывает Bearer в публичном HTTP;
  для постоянной эксплуатации нужен HTTPS, VPN или SSH-туннель.
- Функциональность развёрнута в production образом
  `sha256:ff9163e8c6f14a470714498eb15f58d7acf1e8be713ec778166863bb2e6b5224`;
  volume результатов сохранён, автопарсер выключен, restart policy
  `unless-stopped`. Быстрый откат: контейнер
  `print-factory-panel-pre-api-allowlist-20260822`, image
  `print-factory-panel:backup-pre-api-allowlist-20260822` и `.env`-backup
  `/opt/print-panel/.env.backup-pre-api-allowlist-20260822`.

## Сервисный доступ для внешнего ИИ-ассистента (2026-08-19)

- Production восстановлен после чистой остановки контейнера при перезапуске сервера: `print-factory-panel` запущен, restart policy закреплена как `unless-stopped`, внешний `/health` отвечает 200.
- Машинные клиенты обращаются к `/api/*` с `Authorization: Bearer <PRINT_FACTORY_SERVICE_TOKEN>`. Этот токен не заменяет пароль панели и не даёт доступ к HTML-интерфейсу.
- Секрет создаётся и хранится только в серверном `.env`, минимум 32 символа; его нельзя коммитить, писать в документацию, логи или чат.
- Предпочтительная интеграция Telegram-бота — на том же сервере по внутреннему адресу `http://127.0.0.1:8040`. Удалённому клиенту нельзя передавать Bearer-токен через публичный HTTP: сначала нужен HTTPS либо SSH-туннель.
- Регрессия сервисной авторизации покрыта тестом; изолированный образ прошёл весь `panel/tests/test_app.py` — 14/14.
- Production работает на overlay image `sha256:36693d867e18d10d89f5a64b008bf4040846d29ffb5845002a1a2b69789915e6`; внешний health 200, volume `panel_panel_out` сохранён, автопарсер выключен. Быстрый откат — контейнер `print-factory-panel-pre-service-auth-20260819` и image `print-factory-panel:backup-pre-service-auth-20260819`.

## ROCK SIGNATURE 40: вторая серия в индивидуальном стиле групп (2026-08-13)

- Выпущено ещё 40 принтов: Queen, Metallica, Linkin Park, AC/DC, Король и Шут, Кино, Ария и Пикник — по пять новых сюжетов на группу. В отличие от первой серии, здесь нет общей композиционной сетки: для каждой группы задан отдельный визуальный канон.
- Каноны: Queen — оперный глэм, рояль и сценическая театральность; Metallica — индустриальный трэш-зин; Linkin Park — xerox/stencil, архитектура и звуковые волны; AC/DC — винтажные усилители и высоковольтный hard rock; Король и Шут — хоррор-панк-лубок и тёмная сказка; Кино — строгая позднесоветская линогравюра и ночной город; Ария — героический heavy metal и механические крылья; Пикник — механический сюрреалистический театр.
- Все 40 сцен различаются. Nano Banana рисует иллюстрацию без целевой надписи, название группы накладывается локально точным шрифтом. Промпт запрещает официальные логотипы, копирование обложек, halftone, CGI, псевдотекст и марки производителей на оборудовании.
- Добавлен генератор `generate_rock_signature40.py`: 40 индивидуальных заданий, pilot/resume/only, Pro→Flash fallback, принудительно известный green/blue key и удаление связанной с краем белой кромки. Слабый остаточный alpha до 96 обнуляется для чистого DTF-края.
- Технический итог: 40/40 RGBA PNG 848×1264, alpha 0–255, по 5 файлов на каждую из 8 групп. Созданы 4 обзорных листа, `qc.json` и ZIP.
- Файлы: локально `rock_signature40_20260813/final` и `rock_signature40_20260813/rock_signature40_final.zip`; серверная копия — persistent volume `panel_panel_out/rock_signature40_20260813`.
- При контроле заменены три проблемных результата: AC/DC с самовольным брендом усилителя, Король и Шут без рабочего хромакея и Пикник с фиолетовым вместо чистого синего фона. Queen с внешней белой кромкой исправлен локальной обработкой без нового image-вызова.
- Правовой нюанс прежний: оригинальная генерация не даёт права на официальный мерч. Перед продажей изделий с названиями и узнаваемой айдентикой групп требуется лицензирование/проверка товарных знаков и прав на образы.

## Серия ROCK TOP 40: 8 групп × 5 принтов (2026-08-13)

- Через Nano Banana выпущено 40 финальных принтов: Queen, Metallica, Linkin Park, AC/DC, Король и Шут, Кино, Ария и Пикник — по пять разных композиций на группу.
- Пять механик серии: `frontman`, `lineup`, `myth`, `stage_symbol`, `hybrid`. Это отдельные сюжетные схемы, а не перекраска одного изображения.
- Финальный процесс двухэтапный: Nano Banana создаёт самостоятельную печатную иллюстрацию без букв, затем название группы детерминированно накладывается локальным шрифтом. Это устраняет опечатки и дублирование слов Gemini; для кириллицы добавлен `fonts/FreeSansBold.ttf`.
- Чтобы уменьшить нейросетевой глянец, промпты требуют 3–5 крупных плоских красочных масс, ручной контур, минимум теней и запрещают CGI, HDR, glossy rendering, микротекстуру и halftone. Для проблемных запросов с точными портретами используется оригинальный жанровый архетип без копирования конкретного лица.
- Технический итог: 40/40 PNG, RGBA, 848×1264, alpha 0–255; названия групп точные. Один вариант Linkin Park намеренно имеет более плотную плакатную композицию, остальные преимущественно имеют открытые прозрачные промежутки.
- Результаты находятся локально в `rock_top40_20260813/final`, архив — `rock_top40_20260813/rock_top40_final.zip`; серверная копия хранится в persistent volume `panel_panel_out/rock_top40_20260813`.
- Генератор: `generate_rock_top40.py`, включая продолжение незавершённой серии, выбор отдельных слотов и fallback с Nano Banana Pro на Flash. Финальные 40 файлов получены успешно, но фактический биллинг нельзя выводить только из числа файлов: во время пилотов были отклонённые ответы, повторные попытки и один timeout.
- Перед коммерческой продажей мерча с названиями групп и узнаваемыми образами обязательна отдельная проверка прав, лицензий и товарных знаков. Сам факт генерации не предоставляет лицензию на официальный мерч.

## Рок-группы: коммерческий топ для принтов (2026-08-13)

- Мировой приоритет: Queen, The Beatles, Metallica, Linkin Park, AC/DC, Pink Floyd, The Rolling Stones, Nirvana, Led Zeppelin, Guns N’ Roses.
- Российский приоритет: Король и Шут, Кино, Ария, Агата Кристи, Сплин, Пикник, Алиса, ДДТ, Наутилус Помпилиус, Ленинград.
- Это не единый официальный чарт, а рабочий коммерческий рейтинг: актуальный интерес и стриминг совмещены с исторической узнаваемостью и силой визуального языка для принтов.
- Перед продажей дизайнов с названиями, портретами и узнаваемой айдентикой музыкальных групп требуется отдельная проверка прав и товарных знаков; генератор должен создавать самостоятельную графику, но это само по себе не даёт лицензию на мерч.

## Анти-AI качество рок-стиля 39 (2026-08-13)

- Пользователь принял композиционный язык рок-принтов, но отклонил глянцевый нейросетевой рендер. Канон уточнён: это человеческая печатная графика, а не фотореалистичный concept art.
- Для всех веток обязательны 3–5 крупных плоских красочных масс, экономный ручной контур, несколько осмысленных теней, спокойные поверхности и читаемость с двух метров. Запрещены CGI/3D, HDR-свечение, косметически идеальные лица, повсеместная микротекстура, перегруженная броня, случайные искры и вихри эффектов.
- Контрольная тройка сохранена в `rock_style_tests_20260813`: гитарист в манере ручного рок-плаката, состав группы как цветной cut-paper silkscreen и металлический сюжет как упрощённая пятицветная шелкография. Эти примеры задают новую планку рендера; встроенные контрольные генерации не расходовали баланс Nano Banana пользователя.
- Уточнение выпущено точечным overlay поверх актуального production: image `sha256:972f4ed817c339bc3923b35c76b13243590e660423655808870a01676dc4781b`, health 200, restart count 0. Откат: `print-factory-panel:backup-pre-rock-quality-13cc4ae` и остановленный контейнер `print-factory-panel-pre-rock-quality-13cc4ae`.

## Рок-группы / стиль 39 (2026-08-13)

- Новый банковский стиль `39_rock_band_print` построен по трём отдельным композиционным семьям: `FRONTMAN IMPACT`, `COLOURIZED LINEUP COLLAGE`, `METAL MASCOT NARRATIVE`. Оркестратор назначает их по слотам, затем использует шесть отличающихся гибридов; нельзя полагаться только на случайность Gemini.
- Пользователь просил не переносить халфтон. Канон: чистые печатные цветовые блоки, плакатные тени, выборочные живописные края и лишь контролируемое зерно. Белые прямоугольные дыры, полосы, растянутые пиксели и рваные маски из референсов являются дефектами, а не стилем.
- Введённая тема трактуется как название группы и не проходит через `franchise_scout`; вручную указанный состав сохраняется единым reference-блоком, а не превращается в отдельные несвязанные генерации.
- Один огромный вордмарк группы располагается сверху, пишется точно один раз и получает новые жанровые буквы без копирования официального логотипа. Композиция остаётся фигурным принтом с хромакейными промежутками и отступом 6–8%, без страницы, карточки, стикерного cutline и общей белой подложки.
- Для цветного коллажа состава при зелёном участнике/акценте принудительно использовать синий хромакей. Контрольные файлы: `rock_style_tests_20260813`; GreenKey подтвердил RGBA 1024×1536, alpha 0–255 для всех трёх веток.
- Важно: production 2026-08-13 всё ещё основан на недокументированном image `sha256:3e98ee...`, который не содержит style 38. Рок-изменение разворачивать только overlay-образом поверх текущего production, не заменять его локальной полной сборкой.
- Стиль выпущен точечным overlay из commit `ed971e5`: production image `sha256:f390352e190f966216bc445ab2b688504f6cc4ad98cfbe0a3cc4aa155bc3a8a4`, health 200, restart count 0. Откат: `print-factory-panel:backup-pre-rock-20260813` и `/opt/print-panel-backup-pre-rock-ed971e5`; volume `panel_panel_out` сохранён.

## Мемная серия 20 профессий (2026-08-12)

- Выпущено 20 прозрачных принтов, по одному на профессию, с индивидуальным распределением трёх механик: `gost`, `shift`, `luxury`. Не применять один мемный шаблон ко всем профессиям автоматически.
- Канонические шутки должны опираться на реальные профессиональные ситуации и быть короткими. Иллюстрация создаётся без текста, кириллица и служебные подписи собираются локально.
- При визуальном контроле проверять не только главную надпись, но и текст внутри реквизита. В этой серии английский `CONTRACT` на документе менеджера заменён детерминированной печатью «ОДОБРЕНО» без платного повтора.
- Результаты: `work_memes_20_20260812/final`, 20 PNG 1800×2700, preview, manifest, QC и ZIP. QC 0 дефектов, расход 20 image-вызовов.

## Мемные рабочие стили (2026-08-12)

- Три кандидата: `ГОСТ абсурда`, `Смена затянулась`, `Рабочий люкс`. Это разные механики шутки, а не три цветовых варианта.
- Рыночная отстройка: избегать общих «не хочу работать», «лучший специалист» и подарочного клипарта. Приоритет — профессиональный инсайд, узнаваемая рабочая ситуация и современный статус дефицитного мастера.
- Контрольный пример `ГОСТ абсурда` использует реальный сварочный прикол «электроды по дереву»; `Смена затянулась` переносится почти на любую профессию; `Рабочий люкс` опирается на рост спроса и престижа сварщиков, токарей и электриков.
- Итоги лежат в `work_meme_styles_20260812`, расход — 3 image-вызова. В исходнике №2 обнаруженный английский псевдотекст был полностью перекрыт детерминированной русской версткой без перегенерации.

## 80 принтов / 20 профессий / два стиля (2026-08-12)

- Выпущена каноническая тестовая матрица: 20 профессий, по 4 принта на каждую — 2 конструктивистских и 2 современных. Всего 80 прозрачных PNG 1800×2700.
- Каждый слот имеет отдельное действие героя и отдельный слоган. Nano Banana отвечает только за иллюстрацию без букв; вся кириллица накладывается локально настоящим шрифтом. Этот двухэтапный процесс обязателен для дальнейших текстовых серий.
- Типографика должна быть foreground-слоем поверх героя. Первая сборка показала, что иначе фигура может перекрыть верхние буквы; порядок слоёв исправлен без новых image-вызовов.
- Итоговый QC: 80/80 файлов RGBA нужного размера, прозрачные углы, непустой artwork, 0 технических дефектов. Результаты и два ZIP лежат в `professions_80_20260812/final`; серверные исходники — в persistent volume с тем же именем.
- Фактический расход — 80 image-вызовов Nano Banana, без повторов и ошибок провайдера. Визуальную коммерческую селекцию пользователь выполняет по четырём листам предпросмотра; техническая готовность не означает, что все 80 автоматически являются сильными SKU.

## Выбор стиля профессий (2026-08-12)

- Пользователь выбрал `01_soviet_constructivism` как основное направление серии профессий.
- Переносить из референса нужно художественную систему — три печатные краски, монументальность, диагонали, линогравюрные края и плакатную типографику, — но не советскую государственную символику и не буквальные исторические цитаты.
- Следующий контроль качества: три контрастные профессии до масштабирования на весь список из 20. Это защищает баланс Nano Banana и проверяет универсальность выбранного языка.

## Три эпохи профессий (2026-08-12)

- В дополнение к базовому v2 зафиксированы три стилевых кандидата на одном контрольном сюжете: `01_soviet_constructivism`, `02_retro_tech_1980s`, `03_contemporary_2026`.
- Сравнивать направления нужно на одинаковой профессии и одинаковой фразе — так пользователь оценивает художественный язык, а не привлекательность разных сюжетов.
- Для каждого направления обязательна собственная типографическая система, а не одна надпись поверх разных иллюстраций: диагональная агитационная композиция; CMYK registration/grid; деконструированная крупная современная верстка.
- Прозрачные PNG 1800×2700 и лист сравнения лежат в `profession_three_eras`. Расход — 3 image-вызова. Остальные профессии не масштабировать до выбора направления.

## Качественная планка профессий v2 (2026-08-12)

- Первая серия из 20 технически пригодна, но пользователь не принял её художественное качество. Не масштабировать старый промпт и не считать его товарным эталоном.
- Канонический процесс-кандидат: отдельно генерировать только героя/пластическую иллюстрацию без текста, затем детерминированно верстать настоящим шрифтом точную кириллическую фразу, геометрию и печатные метки. Это устраняет OCR-брак и даёт управляемую иерархию.
- Три прототипа v2: sales, doctor, welder. Сварщик визуально сильнее двух других; перед серийным запуском нужна пользовательская приёмка направления. Старую серию не перезаписывать.
- Технический формат v2: прозрачный PNG 1800×2700, ограниченная палитра, отдельные greenkey-герои и локальная типографическая сборка. Расход прототипа — 3 image-вызова Nano Banana.

## Серия принтов по профессиям (2026-08-12)

- Канонический визуальный подход этой серии: один выразительный специалист по пояс, крупная интегрированная типографика и только 2–4 смысловые профессиональные детали. Не использовать icon pack, sticker sheet, белые cutline-обводки отдельных объектов, прямоугольный постер или журнальную обложку.
- Цветовая система: 3–5 печатных красок, контрастная одновременно на белой и чёрной футболке; хромакей не входит в artwork. После генерации фон удаляется штатным `greenkey_postprocess`.
- Для русского текста недостаточно общей проверки Gemini. После каждого изображения нужен отдельный OCR точной главной фразы; реальные ошибки букв исправлять точечной перегенерацией с короткой построчной раскладкой.
- Принятая серия из 20 прозрачных PNG и лист предпросмотра лежат в пользовательской папке `profession_prints_20260812/final`; серверные исходники — в persistent volume `professions_20260812_direct`.
- Фактический расход: 25 image-вызовов Nano Banana, включая два ранних брака и три исправления текста. Текстовые OCR-вызовы значительно дешевле image generation.
- Важное состояние production: 2026-08-12 обнаружен image `sha256:3e98ee105c70bf3e66ad401fa9b390d64b24ae2a627187085881e74567a827f6`, не совпадающий с последним документированным deploy и не содержащий style 38. Его нельзя перезаписывать без отдельной сверки; в этой задаче production не менялся.

## Молодёжный микс 4/8/5 (2026-08-08)

- Пользователь выбрал из визуального листа направления 4, 8 и 5. Канонический порядок серии: типографический хаос → маркерные детали → диагональная динамика.
- В интерфейсе это один стиль `38_youth_motion_mix` («Молодёжный микс / типографика + маркер + динамика»), но оркестратор назначает каждому изображению отдельный `style_brief`; нельзя оставлять вариативность только на случайность Gemini.
- Первые девять изображений партии имеют разные назначенные композиции и акцентные цвета; затем ротация повторяется. При смешивании с другими стилями счётчик ведётся отдельно только для стиля 38.
- Главный символ всегда зависит от введённой темы. Не сводить серию к одному набору молния/звезда/пламя/череп/город.
- Жёсткий антиформат: никакой журнальной обложки, прямоугольного постера, карточки, общего фона, стикерной подложки или белого cutline. Нужен открытый фигурный принт с хромакейными промежутками.
- Тема обязательна; этот стиль не является автономным, в отличие от авто-стиля 37.
- Стиль выпущен в production из commit `2fdb754`; образ `sha256:67c4d8ff3aecff807531b1e9ad127c76436b2f8cca39941b64345d239e49069e`, health OK, restart count 0, volume сохранён. Откат: `print-factory-panel:backup-pre-youth-mix-2fdb754` и `/opt/print-panel-backup-pre-youth-mix-2fdb754`.

## Единый размер переднего принта (2026-08-03)

- Пользовательская эталонная зона: передний принт должен находиться только в центральной области груди, заметно меньше прежнего full-front, одинаково для всех моделей и чёрной/белой футболки.
- Канонические нормализованные границы на видимой передней панели: X 18–82%, Y 12–70% от воротника к подолу. Оставлять не менее 18% чистой ткани по бокам и 30% снизу.
- Любой artwork, особенно высокий, равномерно уменьшать до попадания внутрь обеих границ с сохранением пропорций. Не расширять его ради заполнения футболки; низ artwork — около пупка/верхней части живота, не у подола.
- Это правило относится только к front-placement; спина остаётся без изменений. Оно должно действовать на всех пяти моделях и обоих цветах футболки.
- Регрессионный prompt-контракт: 13/13 профильных тестов.
- Ограниченная front-zone выпущена в production из commit `0c92471`: image `sha256:3a6ed7f3e1a3e5adfce53e2048999da709a7877623337ae1c24af2f578d54081`, health OK, restart count 0, volume `panel_panel_out` сохранён. Откат: `print-factory-panel:backup-pre-front-zone-0c92471` и `/opt/print-panel-backup-pre-front-zone-0c92471`.

## Реалистичный перенос принта (2026-08-02)

- Пользователь не принимает плоское наложение artwork, похожее на дешёвый Photoshop. Приоритет фотостудии: физически правдоподобная печать, которая следует объёму торса, перспективе, натяжению, складкам, теням и фактуре хлопка.
- Подтверждённый дефект production job `b5506606b205` не вызван режимом Print Factory Signature: похожая плоскость присутствовала в более старом job `69e952ea1c09`.
- Канонический конвейер теперь двухэтапный: (1) blank garment с настоящими складками; (2) localized garment-only transfer по готовой фотографии. Не возвращаться к одному перегруженному Gemini-вызову.
- Второй этап использует существующую яркость ткани как displacement/shading map, сохраняет складки поверх краски и запрещает sticker/poster/rectangle/halo/glossy decal.
- Непрозрачный белый фон удаляется только если он нейтральный и связан с границей. Внутренний белый текст/детали не удаляются; существующий alpha у PNG сохраняется.
- Один итоговый кадр теперь стоит два Gemini image-вызова и генерируется дольше. Пользователь должен видеть это в UI.
- Профильная регрессия: 11/11 тестов. Для точной визуальной оценки нужен живой пользовательский повтор проблемного принта после deploy.
- Двухэтапный перенос выпущен в production из commit `e257d17`: image `sha256:69add5f9d7770f510f778a30857bef8b4b8707a6dcd5f314663177e26ce9c2a7`, health OK, restart count 0, volume `panel_panel_out` сохранён. Откат: `print-factory-panel:backup-pre-two-pass-e257d17` и `/opt/print-panel-backup-pre-two-pass-e257d17`.

## Виртуальная фотостудия (2026-07-30)

- В панель добавлена третья вкладка «Виртуальная фотостудия» для примерки готовых принтов на постоянных синтетических моделях.
- Закреплена молодая совершеннолетняя пятёрка 23–25 лет: Алиса, Майя, София, Артём и Максим. Первый визуально более взрослый черновик отклонён владельцем и в проект не добавлен.
- Рендер использует два Gemini-reference: неизменяемое лицо модели и отдельный PNG принта с alpha. Доступны чёрная/белая свободная футболка, front/back, 1–4 позы, до 6 принтов и максимум 12 кадров за запуск.
- Усилен fabric-integration контракт: принт следует перспективе груди/лопаток, натяжению и складкам, принимает общие тени и фактуру хлопка; запрещены плоская наклейка, ореол и видимая прямоугольная alpha-подложка.
- Добавлен выбранный по умолчанию свет Print Factory Signature: нейтральный ключевой 5000K, сдержанные teal/amber-контуры только по краям силуэта, графитово-дымчатый фон и мягкий ореол. Центр футболки и artwork остаются без цветового сдвига; обычный нейтральный каталог сохранён вторым режимом.
- Фирменный свет выпущен в production из commit fb9e423: image sha256:eacc0e4fd496d4f65c6f1243cdb142f18d6acdfc9cfed517d03d5fd23c265d05, health OK, restart count 0, volume panel_panel_out сохранён. Откат: print-factory-panel:backup-pre-signature-fb9e423 и /opt/print-panel-backup-pre-signature-fb9e423.
- Улучшение выпущено в production из commit `9c99b29`: рабочий image `sha256:9a7eb8019e3adb16cb65722134fefc7270a022b90cdb0a25c8ef8f2b11697cd2`, `/health` — OK, restart count — 0, volume `panel_panel_out` сохранён. Откат: `print-factory-panel:backup-pre-fabric-9c99b29` и `/opt/print-panel-backup-pre-fabric-9c99b29`.
- Платный premium-режим требует отдельного подтверждения в интерфейсе. Pollinations запрещён для этого пути, потому что не сохраняет два референса.
- Фотосессия выполняется в отдельном дочернем процессе: её можно принудительно остановить, готовые кадры скачать отдельно или ZIP.
- Реализация описана в docs/VIRTUAL_STUDIO.md. Профильные тесты: 10/10; Python compile и JS syntax — OK.
- Выпущено в production из commit `7d02dc9`: контейнер работает на image
  `sha256:d51e999a2492e8deca3c65387e2cf5aa63fe4cedc44a83be2b28e062dff1d62e`,
  `/health` отвечает, restart count — 0, постоянный volume `panel_panel_out` сохранён.
- Откат: image `print-factory-panel:backup-pre-studio-7d02dc9` и каталог
  `/opt/print-panel-backup-pre-studio-7d02dc9`.

## Расширенная серия именных мем-принтов «МЕМЯ» (2026-07-29)

- После одобрения тестовой концепции сгенерирован полный локальный набор из 60 самостоятельных вариантов: 30 мужских и 30 женских.
- Популярные имена намеренно повторяются с разными мемными конструкциями: Саня, Никита, Маша, Лена, Катя, Настя и Даша получили по два сюжета.
- Каждый PNG имеет размер 1024×1536 и чистый chroma key green `#00FF00`; мужские и женские исходники разложены в отдельные каталоги `out_batch/memya_60_2026-07-29/male/` и `female/`.
- Зафиксирован полный каталог главных надписей и подзаголовков в `docs/MEMYA_60_CATALOG.md`.
- Серия создана встроенной генерацией изображений по единому контракту: фигурный DTF-силуэт, крупная кириллица, смысловые детали, без мокапов, прямоугольных обложек, логотипов и водяных знаков.

## Серия именных мем-принтов «МЕМЯ» (2026-07-29)

- Подготовлена первая тестовая серия из шести самостоятельных DTF-принтов:
  «МЫША КРАДЁТСЯ», «НАСТЯ, АСТАНАВИТЕСЬ!», «ЛЮБА-БУ», «КОТОСТЬ»,
  «СЛАВА НА СКАКАЛКЕ», «МАКСИМАЛЬНАЯ СИГМА».
- Общий визуальный контракт: оригинальная мемная иллюстрация без копирования
  кадров и персонажей, крупная кастомная кириллица, цельный фигурный контур
  вместо прямоугольной обложки или стикера, молочно-белая графика с
  тёмно-графитовым контуром.
- Исходники сохранены на чистом зелёном фоне для GreenKey в
  `out_batch/memya_2026-07-29/`. Каталог является локальным результатом и
  исключён из Git через `out_batch/`.
- Все шесть надписей визуально проверены на читаемость и корректность кириллицы.

## Постоянная защита расходов Bright Data (2026-07-28, production)

- Production остаётся в `PANEL_RADAR_AUTO_ENABLED=off`; значение по умолчанию в
  `settings.py` также изменено на `off`.
- Даже при будущем включении расписания первый запуск после рестарта ждёт полный
  интервал. Деплой больше не создаёт платный проход через 20 секунд.
- Новый `brightdata_usage` в SQLite — одновременно журнал фоновой задачи и
  атомарный бюджет. Слот резервируется `BEGIN IMMEDIATE` до внешнего POST.
  Успех, ошибка и timeout расходуют слот: программа не может незаметно повторить
  уже потенциально оплаченный snapshot.
- Дневные UTC-лимиты production: 6 TikTok discovery-запросов, 1 выгрузка
  комментариев, 1000 доставленных записей. Тариф оценки UI — $1.5/1000.
- Общий поиск никогда не читает комментарии, независимо от старого значения
  `PANEL_RADAR_COMMENTS_POSTS_PER_RUN`. Комментарии доступны только кнопкой у
  конкретного тренда, после показа количества/максимальной оценки и подтверждения;
  ролики с более чем 500 заявленными комментариями блокируются.
- UI показывает использованные запросы, записи, оценку стоимости и оставшийся
  дневной бюджет. Ручной discovery работает при выключенном расписании.
- Профильные store/collector/API тесты: **55 passed**; `py_compile`, JS syntax и
  `git diff --check` чистые. Полный Windows-набор: **418 passed**, 4 прежних
  multiprocessing/spawn-сбоя генераторных моков; обязательна Linux-проверка.
- Код развернут из commit `e6c0857`; полный Linux-кандидат прошёл **96/96**.
  Production image `sha256:83b176b633ae2a234518227e1460fe231e91e280b8dcc7f5d70d879820af505a`,
  контейнер healthy, 0 рестартов, `/health` → 200.
- В `brightdata_usage` импортированы все фактические 93 snapshots и 15 978
  записей. На текущие UTC-сутки: posts 16/6, comments 12/1, records 4 967/1 000,
  estimate $7.4505; `blocked=true`, оба remaining=0.
- Повторный read-only замер после деплоя снова показал ровно 93 snapshots:
  production не создал ни одного нового Bright Data задания.
- Откат: image `print-factory-panel:backup-pre-budget-e6c0857`, каталог
  `/opt/print-panel-backup-pre-budget-e6c0857`.

## Аварийная остановка расходов Bright Data (2026-07-28)

- После подтверждения владельца production default `PANEL_RADAR_AUTO_ENABLED`
  переключён в `off`; API подтвердил `enabled=false`, `running=false`,
  `next_run_at=null`.
- Аварийный коммит `4f1f480` остановил расписание до постоянного исправления.

## Диагностика расхода Bright Data за ночь (2026-07-28)

- За окно 27.07 18:00 UTC — 28.07 06:13 UTC выполнено 9 полных проходов:
  7 schedule и 2 owner.
- Они создали 93 Bright Data snapshots: 54 TikTok posts discovery и
  39 полных выгрузок комментариев.
- Фактически доставлено 15 978 оплачиваемых записей: 265 постов и
  15 713 комментариев. Крупнейшие comment snapshots: 3 604 и 2 780 записей.
- При публичном PAYG $1.5/1000 успешно доставленных записей валовая оценка
  расхода — около $23.97 до учёта бесплатных кредитов и индивидуального тарифа.
- Причина перерасхода: comments endpoint не имеет лимита количества комментариев
  и возвращает всю ветку; production запускал до 4 таких выгрузок за проход.
- Три таймаута породили дополнительные comment snapshots: бюджет уменьшался
  только после успешного ответа, поэтому следующий ролик запускал новый сбор.
- Планировщик стартует полный проход через 20 секунд после каждого рестарта.
  Несколько production-деплоев сбрасывали трёхчасовой таймер и дали лишние
  schedule-запуски.
- Финансовый API `/customer/balance` вернул 403 для текущего user-token; точную
  сумму списания следует сверять в Bright Data Cost Explorer.
- Диагностика выполнялась read-only; затем расписание аварийно отключено и собран
  deploy-кандидат с постоянным атомарным бюджетом, описанный выше.

## Актуально после порога +3 000 запросов/месяц (2026-07-28)

- К прежней строгой квалификации добавлен отдельный обязательный фильтр:
  `monthly_growth_estimate >= 3_000`.
- Google Trending Now даёт пакетный объём текущего периода, а не точный месячный
  объём Keyword Planner. Поэтому значение честно называется расчётным: для новой
  темы суточный пакет умножается на 30, для ускоряющейся — на 30 умножается только
  прирост между последним и предыдущим замером.
- Расчёт идемпотентен: строится из сохранённых snapshots и не суммирует повторные
  запуски фонового collector.
- API отдаёт `monthly_growth_estimate`, порог, флаг подтверждения и основание
  расчёта; карточка показывает «расчётно +N запросов/мес.».
- Текущие базовые ворота Google фактически строже нового минимума: свежий новый
  сигнал начинается с 500 за сутки (расчётно 15 000/мес.), ускорение — с прироста
  1 000 (расчётно 30 000/мес.). Отдельный порог всё равно зафиксирован в контракте,
  чтобы его можно было независимо менять и тестировать.
- Профильная проверка: **42/42 passed**, `py_compile`, JS syntax и
  `git diff --check` чистые. Полный Linux-кандидат: **90/90 passed**.
- Production выложен из commit `1f744ab`, image
  `sha256:02f84b8e250a2be345fc1466bc75821cf0da3259bdac24f4c6f56b0bc62ab188`;
  контейнер healthy, 0 рестартов.
- Живой API вернул две подтверждённые идеи; у `Odyssey` и `картаполов` расчётный
  рост 15 000/мес., threshold 3 000, `monthly_growth_confirmed=true`.
- Откат: image `print-factory-panel:backup-pre-monthly-growth-1f744ab`, исходники
  `/opt/print-panel-backup-pre-monthly-growth-1f744ab`, SQLite backup
  `trend_radar.sqlite3.pre-monthly-growth-1f744ab`.

## Актуально после исправления пустой выдачи (2026-07-28)

- Нельзя сопоставлять Google и TikTok только по одинаковому написанию. Канонический
  origin хранится в `google_origin_key` у seed и trend.
- Любой дочерний TikTok hashtag/comment seed наследует Google-origin поисковой темы.
  Повторно найденный URL должен прикрепить origin к уже существующему TikTok-тренду,
  а не остаться в старом несвязанном кластере.
- Opportunity использует Google-метрики origin и TikTok-метрики дочернего тренда.
  Строгое двойное доказательство сохранено: Google `>= 500`, два вирусных ролика разных
  авторов, суммарно `>= 100000` просмотров.
- Google-seed с объёмом ниже 500 не расходует лимит TikTok discovery. Явные
  utility-запросы (погода, телепрограмма, списки зачисленных, лунный календарь,
  магнитные бури, курсы валют, одиночные «банк/деньги») также исключены.
- Во время running-прохода пустой блок не является финальным результатом: UI пишет
  «промежуточный экран», показывает `N/6` и обещает автоматическое обновление.
- Каждый проход на 6 тем теперь резервирует 3 слота свежим Google-кандидатам и
  3 слота TikTok/Telegram-ротации: сильный новый Google-сигнал больше не может быть
  вытеснен накопленными TikTok-хештегами.
- Порог Google снижен до ранних 500+ только в связке со строгим TikTok-доказательством;
  один Google-сигнал по-прежнему никогда не создаёт идею для принта.
- Production-репродукция до фикса: 0 qualified при 49 TikTok-трендах; несколько
  дочерних кластеров уже имели достаточную TikTok-виральность, но не видели русскую
  Google-тему.
- Проверки этапа: **40/40** профильных тестов, миграция на копии production SQLite,
  `py_compile`, JS syntax check, чистый `git diff --check`.

- Production выложен из commit `b082a67`, image
  `sha256:00a448e9f9eea64e74e11728e51d5f1ee25811d198d6deb46d9dc2fcfd4af62b`;
  контейнер healthy, 0 рестартов. Linux-кандидат: **89 passed**.
- Контрольный run `2783876f83614a48` завершился `succeeded`: 6/6, 21 seed,
  27 новых сигналов, 3 обновлённых. Появились две подтверждённые идеи.
- Главная проверка lineage: Google «одиссея» связался с TikTok `Odyssey`;
  3 вирусных ролика, 2 автора, 4 322 800 просмотров, confidence 89,9%.
- Откат: image `print-factory-panel:backup-pre-google-quota-b082a67`, исходники
  `/opt/print-panel-backup-pre-google-quota-b082a67`, SQLite backup
  `trend_radar.sqlite3.pre-google-quota-b082a67`.

## Актуально после добавления Google→TikTok возможностей (2026-07-27)

- Основная единица результата радара теперь называется «подтверждённая идея для принта».
  Она требует сразу двух независимых сигналов: нового резкого роста в Google Trends RU
  и вирусных TikTok-роликов.
- Google-доказательство: окно 36 часов, свежесть до 8 часов, `approx_traffic >= 2000`,
  рост между замерами `>= 1.5x`; очень свежий первый официальный RSS-сигнал допустим,
  но первое появление темы только в локальной базе доказательством не является.
- TikTok-доказательство: последние 14 дней, минимум два вирусных ролика разных авторов,
  суммарно не менее 100 000 просмотров; вирусный ролик — 30 000+ просмотров или 300+ репостов.
- История Google хранится в `google_trend_snapshots`; свежие всплески проверяются в TikTok
  раньше широких фоновых seed.
- API подтверждённых идей: `GET /api/radar/opportunities`.
- UI разделяет готовые идеи и кандидатов. Для готовой идеи показывает доказательства,
  уверенность, надпись, образ, композицию и срок выпуска 24–48 часов.
- Нельзя одобрить или отправить в генерацию неподтверждённого кандидата через обычный UI.
- После окончания collector-run карточки и очередь перезагружаются автоматически.
- Проверки этапа: 43 профильных теста, `py_compile`, чистый `git diff --check`,
  браузер без console errors.

- Production-код этапа: commit `8ea040a`, Linux **83/83 passed**.
- Production image:
  `sha256:5bf818a97054f6abe4cc855d817ca997a63e5bb887b67f564672766db9449c6f`,
  контейнер healthy, 0 рестартов.
- Первый плановый проход новой версии `6f47497498df459f` завершился `succeeded`:
  6/6, 20 seed, 27 новых TikTok-сигналов, 3 обновлённых; история Google содержит
  первые сохранённые замеры.
- Откат этапа: image `print-factory-panel:backup-pre-google-tiktok-8ea040a`,
  исходники `/opt/print-panel-backup-pre-google-tiktok-8ea040a`, SQLite backup в volume.

## Актуально после исправления прогресса радара (2026-07-27)

- Production image:
  `sha256:46423a43fdf7289d00585962bf19c821c7499999c31d6203cd193c41b0244bfd`,
  контейнер `print-factory-panel` healthy, 0 рестартов.
- Ранее «зависший шаг» был невидимым последовательным ожиданием Bright Data:
  база обновляла counters только после всего прохода.
- `collector_runs` теперь хранит живые `phase/current_term/steps_total/steps_done`
  и `heartbeat_at`; UI показывает конкретную тему и `N/6`.
- У радара есть отдельная кнопка остановки и
  `POST /api/radar/collector/stop`. Остановка происходит после текущего
  сетевого ответа, максимум в пределах его 75-секундного timeout.
- Реальный контрольный проход `7b1caa2b62fc4d90` доказал движение `0/6 → 3/6`,
  записал 19 seed и 15 TikTok-сигналов, после клика в UI завершился
  `cancelled`; завершённые данные не потерялись.
- Проверки: профильные **36/36**, Linux panel **77/77**. Полный локальный
  Windows-набор дал 400 passed и 4 известные spawn-ошибки monkeypatch; эти же
  четыре теста проходят в Linux.
- Авторасписание включено (`on`), интервал 3 часа.
- Откат: image `print-factory-panel:backup-pre-radar-progress`, каталог
  `/opt/print-panel-backup-pre-radar-progress`.

## Актуально на 2026-07-27

- Финальный production радара: commit `6d622a1`, image
  `sha256:3f1881c6e976a156dc4f989ef89e855cf2d1958b80abedf1b1e325aa7a5763e0`,
  контейнер healthy, Linux **76/76 passed**.
- Живой ротационный проход дал 18 seed и 30 новых TikTok-сигналов.
- Санитарная миграция БД: 83 неподтверждённых `tiktok_hashtag` и 34 старых
  `tiktok_comments` помечены `dismissed`, а не удалены. Подтверждённый двумя
  роликами `kitchenhack` остался активным.
- Откат финального этапа: image
  `print-factory-panel:backup-pre-comment-filter-6d622a1`, каталог
  `/opt/print-panel-backup-pre-comment-filter-6d622a1`.

- Радар больше не должен зацикливаться на четырёх темах. В `radar_seeds`
  хранится ротация: `last_queried_at`, `query_count`, `dismissed`.
- `reject` — постоянное решение: карточка исключается из списка, seed
  блокируется, последующий ingest не снимает отклонение.
- При миграции старые тренды считаются уже проверенными; ранее отклонённые
  темы синхронизируются с `dismissed`.
- Очередь берёт непроверенные темы первой, затем самые давно проверенные.
  Слишком длинные фразы (более 80 символов или 5 слов) в TikTok-поиск не идут.
- Production defaults: 6 TikTok-тем за проход, до 5 роликов на тему,
  комментарии максимум с одного ролика каждой из 4 разных тем.
- Только TikTok-хештеги минимум из двух независимых авторов/роликов и
  подтверждённые минимум тремя авторами новые слова автоматически пополняют
  очередь. Одиночные/короткие общие теги и текущая тема повторно не добавляются.
- При рестарте незавершённый collector-run помечается `failed`.
- Comment-seed допустим только при `observation_count >= 2`, минимум 3 авторах
  и длине от 7 символов. Один ролик не должен порождать поисковую тему.
- Приоритет очереди: подтверждённый TikTok hashtag/comment → Telegram meme →
  широкий Google Trends.
- Карточки показывают ролики и уникальных авторов. Хештеги ролика выделены
  отдельно, общие `#fyp/#viral/#рек` скрываются. Комментарийные слова
  показываются только при score ≥ 35 и минимум трёх авторах.
- Регрессии: постоянный reject, ротация 4+4, фильтр хештегов, распределение
  комментариев и API-скрытие отклонённого. Профильный набор — **31 passed**.
- Полный Windows-набор: **68 passed**, 4 известные `spawn`-ошибки тестовых
  monkeypatch генерации; их обязательно перепроверять в Linux-кандидате.
- Не возвращать интерфейс к формулировке «источников»: при Bright Data это
  всегда один тип источника TikTok и цифра вводит в заблуждение. Показывать
  ролики и авторов.

## Актуально на 2026-07-26

- Bright Data token подключён только через production `.env`, не коммитить и не
  выводить его. Backup env: `/opt/print-panel/.env.pre-brightdata-20260726`.
- Живой TikTok discovery выявил контрактный timeout: три попытки по 30 секунд
  обрывались раньше допустимой минуты ответа. Scrape posts/comments теперь
  выполняется один раз с минимум 75 секундами; progress/download сохраняют
  обычные retry. Регрессионный тест требует ровно `[75]`, а не `[30,30,30]`.
- Исправление выложено из commit `5ea8646`; production healthy на image
  `sha256:7d3a564cc4a0674bd0fa180c9cc4e13800fda34cc90bb43785bc5b5abdcf4e36`.
  Первый живой проход `43646d6841784eca` завершился `succeeded`: 21 seed,
  20 TikTok-сигналов и 222 комментария. Один comments snapshot превысил окно
  опроса, но обход продолжился и комментарии были получены со следующего ролика.
  Откат: image `print-factory-panel:backup-pre-brightdata-timeout-5ea8646`,
  каталог `/opt/print-panel-backup-pre-brightdata-timeout-5ea8646`.
- Радар теперь автоматический:
  - сам запускается после старта и далее каждые 3 часа;
  - бесплатно собирает RU Google Trends и открытый мемный каталог/каналы
    Telegram;
  - сохраняет темы в `radar_seeds`, историю фоновых проходов в
    `collector_runs`, не допускает наложения запусков;
  - TikTok discovery, метрики и комментарии реализованы через Bright Data;
  - живой TikTok-поиск в production настроен через секрет
    `BRIGHTDATA_API_TOKEN`; ключ не хранится в БД/UI/git;
  - повторения новых слов считаются по TikTok-комментариям с уникальными
    авторами, а не по общей частотности слов в Telegram-постах;
  - ручные ссылки оставлены только в свёрнутой диагностике.
- Автоматический API: `GET /api/radar/collector/status`,
  `POST /api/radar/collector/run`, `GET /api/radar/seeds`.
- Кураторские Telegram-seed имеют приоритет при TikTok discovery. Seed не
  считается доказательством новизны: жизненный цикл по-прежнему требует даты,
  независимого источника и/или измеренной скорости.
- Автоматический радар выложен из commit `a14e909`; production healthy на image
  `sha256:ce5aca4d5986b0e860ebde60057f62512272a47bfbe18e850100feed9a309590`.
  Первый production-проход сам собрал 21 seed и завершился `succeeded`.
  Исторический образ до подключения TikTok сохранён для отката: image
  `print-factory-panel:backup-pre-auto-radar-a14e909`, каталог
  `/opt/print-panel-backup-pre-auto-radar-a14e909`.
- Радар мемов теперь должен воспринимать повтор той же ссылки как повторный замер,
  а не как новый ролик и не как бесполезный дубликат. История живёт в
  `metric_snapshots`; отрицательная динамика от уменьшившихся счётчиков запрещена.
- Главный сигнал раннего роста — измеренная скорость за несколько замеров:
  просмотры/лайки/репосты/комментарии в час. Поправки короче пяти минут не должны
  раздувать `velocity_score`.
- Для старого мема новый скачок означает `RESURGENCE`, но никогда не `NEW`/`RISING`.
- Панель принимает пакет до 100 сигналов через JSON и показывает число замеров и
  скорость. Batch API допускает частичный успех.
- Официальный Display API не даёт глобальную выдачу, а Research API не подходит
  коммерческому раннему мониторингу. Поэтому TikTok подключён через разрешённый
  API провайдера данных, а не нестабильный браузерный скрейпинг.
- Этап повторных замеров/скорости выложен из commit `f3cce7e`; Linux-кандидат прошёл
  386/386 тестов. Production healthy на image
  `sha256:bd68e8884edcdfece6e1555e53b7cb5eb7e07bc979f56c4e7e2747cdb7c05b24`.
  Миграция SQLite подтверждена. Откат: image
  `print-factory-panel:backup-pre-radar-velocity-f3cce7e`, каталог
  `/opt/print-panel-backup-pre-radar-velocity-e6646a4`.
- Production: `http://195.133.66.37:8040`, серверный каталог
  `/opt/print-panel`, контейнер `print-factory-panel`, Docker volume
  `panel_panel_out`.
- Панель закрыта паролем; секреты и `.env` не коммитить.
- Генерация использует Gemini, затем штатный GreenKey выдаёт прозрачный PNG.
- Style 37 больше не должен повторять одну машину по запросу марки:
  - ветка `automotive_subject` назначает каждой позиции отдельную конкретную
    модель до независимых вызовов Gemini;
  - встроены каталоги Mercedes-Benz/BMW/Porsche/Ferrari/Audi/Toyota/Nissan/Ford;
  - неизвестным маркам назначаются разные эпохи/категории;
  - точная модель с цифрами сохраняется и получает разные оформления;
  - восемь сеток и восемь смысловых углов ротируются по слотам;
  - текст только в universal neutral dual-contrast: neutral silver/greige,
    charcoal inner stroke + warm-ivory outer keyline; цвет кузова только в
    мелких акцентах.
  - Исправление выложено из commit `74fdb08`; Linux-кандидат прошёл 381/381,
    production healthy на image
    `sha256:61e721013b099949096a09537c2e4df113f46054c93311530b1ea57779b640ca`.
  - Откат: image `print-factory-panel:backup-pre-auto-var-e87b1b5`, каталог
    `/opt/print-panel-backup-pre-auto-var-e87b1b5`.
- Для аниме-принтов ключевое требование — цельная фигурная композиция с
  эффектами и открытым контуром, не прямоугольная журнальная обложка и не
  стикер.
- Добавлен MVP TikTok-first радара:
  - SQLite `PANEL_RADAR_DB` живёт в том же постоянном volume;
  - TikTok — основной тип сигнала, Telegram/YouTube — подтверждения;
  - комментарии учитываются по уникальным авторам, copy-paste штрафуется;
  - первое обнаружение радаром не является датой рождения мема;
  - без даты и второго независимого сигнала только `UNVERIFIED`;
  - старый мем никогда не получает `NEW`/`RISING` из-за одних повторов;
  - старый новый всплеск называется `RESURGENCE`;
  - генерация возможна только после ручного `approve`.
- Важная пользовательская поправка: «Котость» — уже прошедший/зрелый мем,
  не пример раннего роста. Не делать вывод о новизне по популярности или
  сегодняшнему первому появлению в нашей базе.
- Старое ограничение MVP с ручным вводом снято автоматическим collector-ом;
  ручной импорт сохранён только как аварийный диагностический инструмент.
- Новые тесты: 13/13 локально. Интерфейс визуально проверен без платной
  генерации.
- MVP выложен в production 2026-07-26 из commit `7ee87c3`.
  Linux-кандидат прошёл 378/378 тестов; контейнер после переключения healthy.
  База радара находится в `panel_panel_out`.
- Откат: image `print-factory-panel:backup-pre-radar-1ce2d82` и каталог
  `/opt/print-panel-backup-pre-radar-20260726-1ce2d82`.
