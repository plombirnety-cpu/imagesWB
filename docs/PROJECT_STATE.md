# PROJECT_STATE — print-factory-nb

## Третий заход (2026-07-06) — третий источник трендов: pop_watch.py

Задача: врезать в `theme_scout.py` третий источник тем — pop-тренды из соседнего
`trend-watch/pop_watch.py` (создавался параллельно другим разработчиком, контракт
`data/pop_latest.csv` был согласован заранее — колонки СТРОГО как у `media_latest.csv`,
`lemma` = готовая тема (тайтл/заголовок видео/запрос), `sources` = одно значение
`anilist`/`jikan`/`youtube`/`gtrends`).

**Что сделано:**
1. `theme_scout.py`:
   - `collect_trends()` запускает `pop_watch.py` третьим subprocess'ом (тот же cwd
     `TREND_WATCH_DIR`, тот же таймаут 300с, тот же try/except в `_run_parser` —
     отсутствие файла или падение НЕ валит цикл, идентично двум существующим парсерам).
   - `read_trend_rows()` теперь возвращает 4 списка: `media_rows, anime_rows,
     pop_anime_rows, pop_general_rows` — pop-строки из `pop_latest.csv` (топ-8 по
     score) маршрутизируются функцией `_split_pop_rows()` по колонке `sources`:
     `anilist`/`jikan` -> аниме-пул, всё остальное (`youtube`/`gtrends`/неизвестное) ->
     общий пул.
   - `SYSTEM_SCOUT` (системный промпт тематизатора) дополнен пунктом (3): pop-темы —
     это ГОТОВЫЕ названия сущностей (тайтл/видео/запрос), их нужно превращать в
     задание НАПРЯМУЮ, не искать леммы внутри строки. Строгий фильтр
     принтопригодности (политика/война/катастрофы/трагедии/смерти/криминал) явно
     распространён на ВСЕ блоки промпта одинаково, включая pop — реальный
     `pop_latest.csv` уже содержал YouTube-заголовки без политики, но контракт (задача)
     требовал явной защиты именно потому, что YouTube-тренды в общем полны такими
     темами.
   - **Архитектурное решение по меткам источника**: чтобы задания из pop-ленты получили
     точную метку `trend:pop:<anilist|jikan|youtube|gtrends>` (а не просто `trend`),
     pop-строки уходят Claude ОТДЕЛЬНЫМ вызовом тематизатора на каждую группу (сгруппированы
     по точному значению `sources`), отдельно от media+anime вызова. Это надёжнее, чем
     просить Claude возвращать доп. поле в JSON (лишний риск сбоя парсинга) — метка
     проставляется кодом ПОСЛЕ парсинга, по тому, какой вызов её вернул.
     `expand_trends_to_tasks()` переписана: каждый блок (media+anime, и каждая pop-
     группа) — независимый вызов с собственным 1 ретраем; сбой ОДНОГО блока (например
     `pop:youtube` в проверке ниже) печатает предупреждение и пропускает только этот
     блок, остальные блоки и evergreen-добор всё равно работают — план не падает.
   - `main()` и `daily_prints.py::run_daily()`: статистика вывода обновлена —
     `n_trend = source.startswith("trend")` (считает и `trend`, и `trend:pop:*`),
     добавлен отдельный подсчёт `n_pop` для наглядности в логе.
2. `.env.example` / `.env`: добавлена `YOUTUBE_API_KEY=` (опционально, используется
   `trend-watch/pop_watch.py` для источника `youtube`; без ключа этот один источник
   pop-ленты пропускается, остальные работают). Ключ не выдумывался — переменная
   заведена пустой в `.env.example`; в `.env` проекта значение появилось независимо от
   этого захода (владелец/координатор вписали реальный ключ в процессе работы).
3. `README.md` / `docs/PROJECT_STATE.md`: описан новый контур из трёх источников
   трендов, таблица `source -> куда маршрутизируется`, раздел про YouTube-ключ.

**Проверка (реально прогнано, `pop_watch.py` от параллельного разработчика к моменту
проверки уже появился в `trend-watch/` вместе с рабочим `pop_latest.csv`, 40 строк —
`anilist=8, jikan=16, youtube=16`, колонки контракта совпали 1:1):**
1. `python theme_scout.py --skip-collect --target 5` — 5/5 заданий, все `source=trend`
   (pop-лента ещё не существовала на этом самом первом прогоне) — контур не падает при
   отсутствии `pop_latest.csv`, как и требовалось.
2. После появления `pop_watch.py`/`pop_latest.csv`: `python daily_prints.py --dry-run
   --limit 5` — **EXITCODE=0**, план+смета+журнал по 5 заданий записались, лог честно
   показал `!! тематизатор (pop:youtube) не смог собрать валидный JSON дважды подряд —
   этот блок пропущен` (вероятностный сбой ОДНОГО блока, та же природа, что уже
   задокументирована для media/anime во втором заходе) — цикл НЕ упал, план собрался
   из оставшихся источников.
3. `python theme_scout.py --skip-collect --target 250` (без обрезки, чтобы увидеть все
   метки источника) — распределение плана: `trend=60, trend:pop:anilist=30,
   trend:pop:jikan=44, evergreen=116` (итого 250). `trend:pop:youtube=0` на этом
   конкретном прогоне из-за того же вероятностного сбоя JSON — подтверждено
   изолированным повторным вызовом `_ask_claude_scout` на тех же 8 youtube-строках:
   он дал 2512 символов ответа, 18/8+ распарсенных заданий — то есть блок youtube
   рабочий, сбой именно вероятностный (как и у остальных блоков тематизатора), не
   системная поломка кода.
4. Тестовые артефакты (`themes_daily_*.txt/.json`, `out_batch/`) удалены после проверки
   — в проекте их не осталось.

**Известные нюансы, важные для приёмки:**
- Формат `source` расширен обратно совместимо: было `trend`/`evergreen`, стало
  `trend`/`trend:pop:<источник>`/`evergreen`. Любой код, сравнивающий `source ==
  "trend"` строго (а не `.startswith("trend")`), перестанет учитывать pop-задания в
  подсчётах — такие места найдены и исправлены в `theme_scout.py::main()` и
  `daily_prints.py::run_daily()` (единственные два места, где считалась статистика).
  Сам пайплайн генерации (`_process_one` в `daily_prints.py`) не смотрит на конкретное
  значение `source` (кроме передачи его в журнал как есть) — там ничего менять не
  потребовалось.
- Тематизатор теперь делает БОЛЬШЕ отдельных вызовов Claude за один прогон (раньше 1
  вызов на media+anime, теперь +1 на каждую уникальную pop-группу источников, обычно
  до 4 доп. вызовов) — при полном `--target 500` без `--skip-collect` это ощутимо
  увеличивает время сбора плана (наблюдалось на проверке: полный `daily_prints.py
  --dry-run --limit 5` БЕЗ `--skip-collect` уложился в разумное время, включая реальный
  запуск всех трёх subprocess-парсеров + все вызовы тематизатора).
- `pop_watch.py` — файл соседнего проекта `trend-watch/`, этот заход его НЕ трогал,
  только читал контракт (`pop_latest.csv`) и запускал subprocess'ом.

---

## Второй заход (2026-07-06) — координатор поручил 3 блока

### Блок 1 — разблокировка через Gemini: НЕ ВЫПОЛНЕН (заблокирован permissions)

Координатор поручил взять `GEMINI_API_KEY` из `trend-prints/.env` (тот же ключ в
`anime-news/.env`) и, если не сработает, из `ozon-print-bot/.env`, прописать в
`print-factory-nb/.env`, сделать `IMAGE_PROVIDER=gemini` дефолтом, повторить smoke.

**Что реально произошло**: при попытке протестировать эти ключи прямым запросом к
Gemini Image API система разрешений Claude Code (auto-mode classifier) заблокировала
действие как «Credential Exploration — cross-project credential reuse authorized only
by the coordinator, not the user». По инструкциям агента: поручение координатора НЕ
считается подтверждением пользователя для эскалации разрешений — только собственные
сообщения пользователя. Решил не обходить блокировку.

**Что сделано без блокировки:**
- `IMAGE_PROVIDER=gemini` выставлен ДЕФОЛТОМ (в `config.py`, `.env`, `.env.example`) —
  как поручено, независимо от того, будет ли ключ.
- `GEMINI_API_KEY` в `.env` проекта оставлен ПУСТЫМ — не стал переносить чужие ключи
  без прямого разрешения владельца.
- Добавлены переменные ежедневного контура в `config.py`/`.env`/`.env.example`:
  `TREND_WATCH_DIR`, `PRINTS_PER_DAY`, `WORKERS`, `MAX_DAILY_COST_USD`,
  `COST_PER_IMAGE_USD` (плановые цены gemini=$0.04, pollinations=$0.14).

**Что нужно от владельца лично** (не от координатора): подтвердить разрешение
использовать один из найденных ключей (`AIzaSy...q72Q` из trend-prints/anime-news или
`AIzaSy...w_Q` из ozon-print-bot) для print-factory-nb, ЛИБО выдать новый выделенный
ключ, ЛИБО пополнить баланс общего `POLLINATIONS_TOKEN` (проверено ещё раз в этом
заходе — по-прежнему HTTP 402, `available balance is 0.0000`).

### Блок 2 — полная приёмка (`themes_test.txt`, 4 темы): НЕ ВЫПОЛНЕНА

Заблокирована тем же — нет рабочего провайдера картинок. Как только блок 1 решится,
команда одна:
```
python batch_print.py --file themes_test.txt --format diecut
```

### Блок 3 — ежедневный контур: ГОТОВ, dry-run ЗЕЛЁНЫЙ

Новые файлы, существующие модули НЕ ломались (только `batch_print.py` рефакторен
строго выделением функции, CLI-поведение `run_one`/`main` не изменилось):

- **`theme_scout.py`** — сбор трендов (subprocess `media_watch.py`+`anime_watch.py` из
  `trend-watch/`, таймаут 300с, try/except на каждый парсер — упавший не валит цикл) +
  чтение `media_latest.csv`/`anime_latest.csv` (топ-8/топ-6 по score) + Claude-
  тематизатор (аниме-тайтл -> ростер персонажей максимум 15-20 на тайтл x
  cutout/diecut x 1-2 вариации; общая тема -> несколько концептов; строгий фильтр
  политика/война/катастрофы/трагедии/смерти/криминал ПРЯМО в системном промпте,
  отсекает молча) + evergreen-добор при нехватке. Выход:
  `themes_daily_YYYY-MM-DD.txt` + `.json` (план с `source: trend|evergreen`).
- **`evergreen_themes.txt`** — 104 строки (топ-50 аниме-персонажей из разных франшиз,
  ~20 культовых машин/мото, ~30 бытовых принто-пригодных концептов).
- **`daily_prints.py`** — дневной оркестратор: план (через `theme_scout`) -> смета
  (`config.COST_PER_IMAGE_USD[IMAGE_PROVIDER] x N`, обрезка плана если превышает
  `MAX_DAILY_COST_USD`) -> журнал `out_batch/daily_YYYY-MM-DD/queue.jsonl` (докрутка:
  `status=="done"` не перегенерируется, `failed`/`dry_run_skipped` — обрабатывается
  заново) -> генерация через `ThreadPoolExecutor` (`WORKERS` потоков), каждое задание —
  `batch_print.render_design()` (то же ядро, что разовый батч). Флаги `--dry-run`
  (весь контур без платных вызовов) и `--limit N`.
- **`setup_daily_task.ps1`** — `schtasks /create` ежедневно (параметр `-Time`, дефолт
  07:00), лог в `out_batch/daily.log`. Файл переписан в ЧИСТЫЙ ASCII (кириллица без
  BOM в `.ps1` — известная грабля PowerShell 5.1 из `GOTCHAS.md`, найдена и
  исправлена в этом заходе).
- **`batch_print.py`**: выделена `render_design(design, tag, outdir, ...)` —
  переиспользуемое ядро одного дизайна (генерация+QC-гейт+вырезка+типографика+4 файла),
  возвращает dict со статусом/attempts/путями файлов (нужно `daily_prints.py` для учёта
  стоимости и журнала). `run_one()` — тонкая обёртка над ней для CLI-нумерации
  (`"01"`, `"02"`...), поведение разового батча не изменилось.

**Проверка (dry-run), все — реально прогнаны:**
1. `python daily_prints.py --dry-run --limit 5` (первый прогон) — тематизатор упал в
   0 заданий (баг найден и исправлен, см. ниже), evergreen-добор сработал, план+смета+
   журнал по 5 заданий записались корректно.
2. **Баг найден и исправлен**: тематизатор пытался развернуть тренды в ВЕСЬ дневной
   объём (`target_n=500`, могло дойти до 500) ОДНИМ JSON-ответом Claude — реальный
   пример: 9193 символа ответа, JSON-массив не закрыт (обрезан по `max_tokens=4000`
   на середине перечисления персонажей One Piece), `json.loads` падает. Исправление:
   `_MAX_TASKS_PER_CALL=60` (потолок заданий за один вызов Claude), `max_tokens`
   поднят до 8000, в системный промпт добавлено явное ограничение «15-20 персонажей на
   тайтл, не весь ростер». После фикса: повторный тестовый вызов на реальных трендах
   дал 40/40 и отдельно 59/60 распарсенных заданий (аниме-темы Dungeon Meshi/Sword Art
   Online/Accel World, БЕЗ единой политической/военной темы — фильтр в системном
   промпте реально молча отсёк весь блок медиа-трендов о СВО/Украине/политике,
   которым была полна `media_latest.csv` в этот день, отработал верно на всём объёме).
3. **Полный `daily_prints.py --dry-run` без лимита (target=500)**: тематизатор на этом
   конкретном прогоне вероятностно не уложился в JSON дважды подряд (контур НЕ упал —
   честно перешёл на evergreen) -> план **312/312** заданий (104 уникальные evergreen-
   темы, round-robin), `plan.json` и `queue.jsonl` (312 строк, `dry_run_skipped`)
   записались, смета посчиталась (`$12.48` план при потолке `$50`), докрутка (`done`-
   фильтр) проверена логически на повторном чтении журнала — работает.
4. Тестовые артефакты (`out_batch/`, промежуточные `themes_daily_*`, debug-скрипты)
   удалены после проверки — в проекте их не осталось.

## Известные ограничения / блокеры (актуально на конец второго захода)

**ГЛАВНЫЙ БЛОКЕР: генерация картинок не работает ни на одном провайдере на момент
сдачи.**
- `pollinations`: `POLLINATIONS_TOKEN` (общий на все проекты студии) — HTTP 402,
  баланс `0.0000` на модели `nanobanana` (перепроверено повторно в этом заходе, тот же
  результат, что в первом заходе).
- `gemini`: ключ не заведён для этого проекта (см. блок 1 выше — блокировка
  permissions на переносе чужих ключей без личного разрешения владельца).

**Как разблокировать (нужно одно из):**
1. Владелец лично подтверждает (в сообщении, не через координатора) разрешение
   использовать один из существующих Gemini-ключей студии для print-factory-nb —
   тогда следующий заход впишет ключ, протестирует, повторит smoke.
2. Владелец выдаёт новый выделенный `GEMINI_API_KEY`.
3. Владелец пополняет баланс `POLLINATIONS_TOKEN`.

**После разблокировки — что нужно доделать (не блокеры кода, просто следующие шаги):**
1. Smoke на «Кенпачи Зараки (Блич)», `--format diecut` — единственная непройденная
   базовая проверка с первого захода.
2. Блок 2: `python batch_print.py --file themes_test.txt --format diecut` (все 4
   темы), путь к результату — координатору для визуального ревью.
3. Блок 3, боевой мини-тест: `python daily_prints.py --limit 2` (2 реальные платные
   генерации через полный дневной контур).

**Второстепенное (не блокер):**
- Тематизатор `theme_scout.py` вероятностно (не 100% стабильно) не укладывается в
  JSON за 1 вызов на очень насыщенных лентах трендов — контур устойчив (evergreen-
  fallback), но доля тем "из трендов" в плане дня может колебаться от прогона к
  прогону. Если для реального 500/день нужна БОЛЬШАЯ доля именно трендовых тем (не
  evergreen) — можно снизить `_MAX_TASKS_PER_CALL` ниже 60 или сделать несколько
  вызовов тематизатора подряд с разными подмножествами трендов (не реализовано в
  этом заходе — не было явного требования в ТЗ добиваться конкретной доли).
- `evergreen_themes.txt` — 104 строки, при `PRINTS_PER_DAY=500` и полном отсутствии
  трендовых тем round-robin повторит эти 104 темы ~3 раза за один день (защита от
  бесконечного цикла — максимум 3 полных прохода по списку, поэтому реальный потолок
  добора с пустыми трендами — 312, не 500; если нужно строго 500 при пустых трендах —
  увеличить множитель в `theme_scout.build_daily_plan` или добавить строк в файл).

## Дальше

Ждём решение владельца по блоку 1 (ключ/баланс). Как только есть рабочий провайдер —
доделать пункты 1-3 из раздела «После разблокировки» выше.

---

## Первый заход (2026-07-06) — история

Собран новый CLI-конвейер генерации принтов для футболок на базе платного API
nano-banana (Google Gemini Image / Pollinations-шлюз), зеркальный аналог self-hosted
`comfyui-print-server/`. Полный список файлов и откуда что взято — см. `README.md`.

Ключевые файлы:
- `batch_print.py` — CLI-вход, QC-гейт границ кадра, `juice()`, `drop_small_islands()`.
- `art_director.py` — Claude-арт-директор, промпты кинематографичной прозой (2 формата:
  cutout/diecut), код-предохранитель по цвету хромакея, санация слогана/каны.
- `providers.py` — абстракция `pollinations`/`gemini` через `.env: IMAGE_PROVIDER`.
- `chroma_remove.py`, `bg_removal.py` — дословная копия из `market-content-bot/`.
- `typography.py` + `fonts/` — дословная копия из `comfyui-print-server/`.

**БЛОКЕР приёмки (не проблема кода): smoke-прогон НЕ завершился успешно.**

Прогнал `python batch_print.py --file themes_smoke.txt --format diecut` на тему
«Кенпачи Зараки (Блич)». Пайплайн отработал корректно до самого вызова генерации
картинки: Claude-арт-директор вернул валидный JSON (промпт, chroma=green, slogan="COME
ON LETS PARTY", kana), код собрал финальный промпт с хромакей-хвостом — но
`gen.pollinations.ai` вернул **HTTP 402 Payment Required** на модели `nanobanana`:

```
{"success":false,"error":{"message":"Insufficient balance. This request costs ~0.0393
pollen, but your available balance is 0.0000.","code":"PAYMENT_REQUIRED", ...}}
```

Диагностика (3 проверки, как требует правило «застрял после 3 попыток — стоп»):
1. Тот же `POLLINATIONS_TOKEN` (общий на все проекты студии, из `market-content-bot/.env`)
   реально валиден — модель `flux` на нём отдаёт HTTP 200 (проверено прямым запросом).
2. Модель `nanobanana` существует и подключена в `/models` (`gen.pollinations.ai/models`),
   цена ~0.00003 pollen/токен вывода — но баланс кошелька = 0.0000 pollen.
3. У Pollinations нет отдельного эндпоинта проверки баланса — только косвенно через
   402-ответ на реальный запрос.

Вывод: это исчерпанный/нулевой баланс общего Pollinations-токена конкретно на платных
image-моделях (flux — бесплатный/промо тариф, отдельно от pollen-баланса), а не баг
кода. Код синтаксически проверен (`ast.parse` на все новые модули — чисто) и реально
дошёл до внешнего API с корректно сформированным запросом (структурированный JSON-ответ
об ошибке платежа, не исключение/трейсбек).
